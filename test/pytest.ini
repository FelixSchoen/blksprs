[pytest]
log_cli = 1
log_level = INFO
log_cli_format = %(asctime)s - [%(name)s] - %(levelname)s: %(message)s
pythonpath = ..
markers =
    benchmark: high-memory or high-runtime regression tests
filterwarnings =
    always
